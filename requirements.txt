# Runtime dependencies (Python 3.11+)
# aiohttp ships with discord.py and is used for the Crafty and Azure REST calls.
discord.py>=2.4,<3
# 3.8 is the floor so a Raspberry Pi Zero W can reuse Debian's prebuilt
# python3-aiohttp package instead of compiling the C extensions on ARMv6.
aiohttp>=3.8,<4
python-dotenv>=1.0,<2

# Azure credentials via the official SDK (DefaultAzureCredential).
# Optional: on a Raspberry Pi Zero W (ARMv6) `cryptography` has no wheel, so you
# can skip this line and use service-principal credentials instead -- see the
# README section "Azure authentication on a Pi Zero W".
azure-identity>=1.16,<2
